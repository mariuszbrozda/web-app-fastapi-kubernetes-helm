apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "online-store.fullname" . }}-test-connection"
  labels:
    {{- include "online-store.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "online-store.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
